server:
  port: 8300   #端口

spring:
  application:
    name: service-manger-center #应用名称

eureka:
  instance:
    hostname: 127.0.0.1 #服务名称
  client:
    register-with-eureka: false #不向注册中心注册自己
    fetch-registry: false #不检索服务
    #service-url:
      #defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/